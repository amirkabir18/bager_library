# راهنمای وظایف دانشجویان - پیاده‌سازی رابط کاربری با PySide6

این سند نقشه راه و وظایف مشخص هر یک از سه دانشجو را برای تکمیل صفحات نسخه PySide6 سامانه مدیریت کتابخانه مشخص می‌کند.

---

## 🚀 راهنمای راه‌اندازی و اجرای پروژه

1. **محیط مجازی:**
   ```powershell
   .venv\Scripts\activate
   ```
2. **اجرای نسخه جدید (PySide6):**
   ```powershell
   python main_pyside.py
   ```
3. **اجرای نسخه قبلی (جهت مقایسه و بررسی عملکردها):**
   ```powershell
   python main.py
   ```
4. **اجرای تست‌های پروژه:**
   ```powershell
   pytest
   ```

---

## 🎨 راهنمای استاندارد طراحی و کدهای رنگی (Theme)
کلیه رنگ‌ها، فونت‌ها و آیکون‌ها در فایل `ui_pyside/theme.py` تعریف شده‌اند. **از نوشتن رنگ‌های هاردکد خودداری کنید**:

- **رنگ اصلی برند:** `COLOR_PRIMARY = "#2945D3"`
- **رنگ آبی تیره هدر:** `COLOR_PRIMARY_DARK = "#172B8F"`
- **رنگ پس‌زمینه هاور / چیپ‌ها:** `COLOR_PRIMARY_LIGHT = "#EAF0FF"`
- **رنگ تأکیدی دکمه‌ها:** `COLOR_ACCENT = "#FFC400"`
- **رنگ متن اصلی:** `COLOR_TEXT_MAIN = "#172033"`
- **رنگ خطوط و مرزها:** `COLOR_BORDER = "#E2E8F0"`
- **فونت سیستم:** `IRANSansWeb(FaNum)` با استفاده از `get_app_font(size, weight)`
- **آیکون‌ها:** تابع `get_icon(name, color_hex, size)` برای دسترسی به آیکون‌های لوسید در `assets/icons/lucide/`

---

## 👤 دانشجوی شماره ۱: مدیریت کتب و رده‌بندی هوشمند دیویی

### فایلهای تحت مسئولیت:
- `ui_pyside/views/books_view.py`
- `ui_pyside/views/categories_view.py`
- دیالوگ‌های افزودن/ویرایش کتاب در `ui_pyside/dialogs/book_dialogs.py`

### وظایف و امکانات الزامی:
1. **پنجره افزودن و ویرایش کتاب (`BookEditDialog`):**
   - فیلدهای ورودی: عنوان کتاب، نویسنده، شابک (ISBN)، رده دیویی، شرح رده، تعداد نسخه.
   - دکمه **«جستجوی خودکار شابک و رده‌بندی»**:
     - با وارد کردن شابک و کلیک، متد `book_service.lookup_isbn_and_classify(isbn)` از `services/book_service.py` فراخوانی شود و فیلدهای عنوان، نویسنده و کد دیویی به صورت خودکار پر شوند.
   - دکمه **«پیشنهاد هوشمند رده‌بندی با هوش مصنوعی»**:
     - اتصال به `services.dewey_ai_agent` برای استخراج کد دیویی بر اساس عنوان و نویسنده.
   - ذخیره و اعتبارسنجی در جدول `books` در پایگاه‌داده SQLite.
2. **عملیات سطرهای جدول کتب:**
   - اتصال دکمه‌های آیکونی هر سطر (مشاهده جزئیات، ویرایش، حذف با تأییدیه).
   - پیاده‌سازی پیش‌نمایش تصویر جلد کتاب (بارگذاری فایل تصویر و نمایش در سطر).
3. **خروجی اکسل/CSV:**
   - دکمه صدور خروجی CSV از کتاب‌های فیلتر شده با متد `database.write_csv_file`.
4. **صفحه رده‌بندی دیویی (`CategoriesView`):**
   - نمایش لیست رده‌های اصلی ده‌گانه دیویی (۰ تا ۹۰۰) با استفاده از `config/dewey.py` یا `data/dewey_data.json`.
   - نمایش تعداد کتاب‌های موجود در هر رده و فیلتر کردن کتاب‌ها با کلیک روی هر رده.

---

## 👤 دانشجوی شماره ۲: امانات، بازگشت‌ها و یادآوری‌ها

### فایلهای تحت مسئولیت:
- `ui_pyside/views/loans_view.py`
- دیالوگ‌های ثبت و تمدید امانت در `ui_pyside/dialogs/loan_dialogs.py`

### وظایف و امکانات الزامی:
1. **داشبورد مدیریت امانات (`LoansView`):**
   - طراحی تب یا دکمه‌های سوئیچ بین:
     - **امانات جاری:** کتبی که در حال حاضر در دست اعضا هستند (`borrowed = 1`).
     - **آرشیو بازگشت‌ها:** تاریخچه امانات تسویه‌شده (`borrowed = 0`).
   - جدول با ستون‌های: شناسه، نام کتاب، نام عضو، تاریخ امانت، تاریخ سررسید، روزهای باقی‌مانده / تاخیر، وضعیت (چیپ سبز «عادی» یا چیپ قرمز «دارای تاخیر»).
2. **پنجره ثبت امانت جدید (`AddLoanDialog`):**
   - انتخاب کتاب: کومبوباکس با قابلیت جستجو از میان کتب موجود (`borrowed = 0`).
   - انتخاب عضو: کومبوباکس جستجوپذیر از میان اعضای فعال کتابخانه.
   - تاریخ امانت: تاریخ جاری به صورت شمسی با کتابخانه `jdatetime`.
   - مدت امانت: انتخاب تعداد روز (پیش‌فرض ۱۴ روز).
   - کنترل سقف مجاز امانت: بررسی عدم تجاوز تعداد امانات کاربر از سقف مجاز تنظیم‌شده در سیستم.
3. **عملیات بازگشت و تمدید کتاب:**
   - دکمه **«ثبت بازگشت کتاب»**: به‌روزرسانی وضعیت امانت و ثبت تاریخ بازگشت در دیتابیس.
   - دکمه **«تمدید امانت»**: افزایش تاریخ سررسید به مدت ۷ یا ۱۴ روز دیگر.
4. **ماژول اعلان و ارسال پیام یادآوری:**
   - اتصال به `notifications.LoanReminderManager` و دکمه «بررسی و ارسال یادآوری تاخیر».
   - نمایش لاگ ارسال یادآوری به اعضا.

---

## 👤 دانشجوی شماره ۳: اعضای کتابخانه، کاربران سیستم و تنظیمات

### فایلهای تحت مسئولیت:
- `ui_pyside/views/members_view.py`
- `ui_pyside/views/users_view.py`
- `ui_pyside/views/settings_view.py`
- دیالوگ‌های کاربر و عضو در `ui_pyside/dialogs/user_dialogs.py`

### وظایف و امکانات الزامی:
1. **مدیریت اعضای کتابخانه (`MembersView`):**
   - جدول اعضا با ستون‌های: شناسه، نام و نام خانوادگی، شماره تماس، تعداد کتب در امانت، سابقه، عملیات.
   - دیالوگ افزودن و ویرایش مشخصات عضو به همراه اعتبارسنجی شماره تلفن همراه با `normalize_phone_number`.
   - دیالوگ «کارنامه / سوابق عضو»: نمایش لیست تمام کتبی که عضو تا کنون به امانت برده است.
2. **مدیریت کاربران سامانه و سطوح دسترسی (`UsersView`):**
   - جدول کاربران سیستم (`auth_users`) با نمایش نقش‌ها (مدیر ارشد، مدیر، کتابدار).
   - دیالوگ ایجاد کاربر جدید با رمز عبور هش‌شده امن (`auth.py`).
   - تغییر وضعیت فعال/غیرفعال کاربر و بازنشانی گذرواژه.
   - پاپ‌اور پروفایل کاربر جاری در هدر برای مشاهده مشخصات و تغییر رمز عبور خود کاربر.
3. **صفحه تنظیمات و اعلان‌ها (`SettingsView`):**
   - فرم تنظیمات اعلان‌ها: توکن ربات تلگرام، شناسه چت و فعال‌سازی سرویس پیامکی.
   - بازه زمانی یادآوری سررسید (مثلاً ۲ روز قبل از سررسید) و سقف امانت همزمان.
   - جدول تاریخچه اعلان‌ها (`notification_logs`) با قابلیت جستجو و فیلتر.

---

## ✅ چک‌لیست تحویل نهایی دانشجویان

- [ ] برنامه بدون خطا و با دستور `python main_pyside.py` اجرا شود.
- [ ] چیدمان کاملاً راست‌به‌چپ (RTL) رعایت شده باشد.
- [ ] تمامی دستورات پایگاه‌داده از طریق `get_db_connection(db_p)` به صورت تراکنشی (Commit / Rollback) مدیریت شوند.
- [ ] از استایل‌ها و پالت رنگی `design.md` و `ui_pyside/theme.py` تبعیت شود.
- [ ] تست‌های موجود (`pytest`) همگی سبز بمانند.
